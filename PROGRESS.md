# Progress

- [x] MWR-1 単一immutable input resourceの公開identity・bounded private admission・固定environment・既存worker lifetimeへの統合を実装し、admission 15 tests、Jetson native C backend成功/拒否経路、および同一designerの契約reviewを完了 `depends:none` `parallel:none` `commit:2ddbc78`
- [x] MWR-2 MWR-1の公開契約、source audit、focused unit/integration tests、既存resource-free worker回帰、resource-required factoryの実process成功/欠落失敗、Macとnative Linuxの実worker acceptanceを統合し、protocol v1・factory ABI・W2 strict bundle layout・deadline/cancel/crash isolationと下流consumerのfilesystem/POSIX非依存を確認。native Linux direct compile/linkは[3,4]→[4,6]、graceful shutdown、missing resourceの`sessionUnavailable`、exit 0を確認し、Sol migration reviewを完了 `depends:MWR-1` `parallel:none` `evidence:../Lume/.build/linux-worker-proof/native-runtime.log,../Lume/.build/linux-worker-proof/native-runtime-result.json`
- [x] MWR-3 multiple verified resource admissionをcaller-owned explicit count/aggregate-byte limits、canonical set identity、private immutable staging、部分失敗rollbackとして実装し、37 focused testsとMac/native Linux public-client lifecycle acceptance、同一Solのfinding限定再確認をPASSした。CPU Mojo fixtureの証拠は`.build/w3-multiple-resource-acceptance/acceptance-result.json`。本commitで確定し、先行21 commitsのため自動pushしない `depends:MWR-2` `parallel:none`
- [x] MWR-4 現source/consumer SHAとMac/native Linuxの2resource成功・typed拒否・graceful/timeout/cancel時PID回収/stage cleanup/fresh attemptを統合確認し、Lume `LMAX-2.1`へ返却した。broad suiteは外部compiler/build deadlineとcleanup EPERMでfailedのままであり、37 focused testsと実worker acceptanceを全suite成功やMAX inferenceへ一般化しない `depends:MWR-1,MWR-2,MWR-3` `parallel:none`

- [x] DIRECT-1 Define and validate host-view and factory-provenanced opaque-resource contracts using existing ownership; source signatures, graph relationships and native adapters are qualified `depends:none` `parallel:none` `commit:10718c2`
- [x] DIRECT-2 Implement direct Span/MutableSpan and aggregate resource calls; validate provenance, shared admission, completion on all statuses and destruction with 394 package tests plus actual Mac/Jetson calls and Swift-side ASan `depends:DIRECT-1` `parallel:none` `commit:10718c2`
- [x] DIRECT-3 Qualify host and opaque-resource boundaries against their direct C ABI dispatchers; preserve CPU timing/allocation provenance and limits in Benchmarks/RuntimeBridge/results `depends:DIRECT-2` `parallel:none` `commit:10718c2`
- [ ] DIRECT-RELEASE Publish the qualified direct API as 0.3.0 before changing Lume `depends:DIRECT-1,DIRECT-2,DIRECT-3` `parallel:none`
  - [ ] DIRECT-RELEASE.1 Complete immutable consumer/release gates with the user-approved Swift 6.4 minimum: qualified direct implementation remains 10718c2; standard SwiftPM execution replaces the mismatched Xcode test runner, cold external-product builds have a separate bounded gate; all 393 local tests pass in complementary bounded groups after synchronizing process fixtures, and tagging waits for final CI and remote consumer checks `depends:DIRECT-1,DIRECT-2,DIRECT-3` `parallel:none`
  - [ ] DIRECT-RELEASE.2 Publish and verify the 0.3.0 tag and exact-version consumer, then publish the release notes `depends:DIRECT-RELEASE.1` `parallel:none`
- [ ] DIRECT-INTEGRATION Verify the published release and complete direct API evidence together before entering Lume implementation `depends:DIRECT-1,DIRECT-2,DIRECT-3,DIRECT-RELEASE` `parallel:none`
